// Declara que esta interface pertence ao pacote "calculos"
package calculos;

// Define uma interface chamada "Classificavel"
public interface Classificavel {

    // Método abstrato que deve ser implementado pelas classes que implementam esta interface.
    // O método retorna um valor inteiro que representa a classificação do item (por exemplo, de 0 a 10).
    int getClassificacao();

}
